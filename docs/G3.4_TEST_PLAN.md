# G3.4 Test Plan

## Pipeline

`PlayAi -> GuardAi -> DecisionFabric -> RuntimeAdapter -> DryRunRuntime`

## Required checks

1. Unit tests for runtime contracts and command mapping.
2. Deterministic dry-run execution.
3. Full pipeline simulation with no external side effects.
4. NosTale adapter hard-lock verification.
5. Empty/invalid action rejection.
6. CI regression suite and quality gate.

## Safety assertion

The integration test must prove that the prepared NosTale adapter cannot execute
real actions in G3.4.

## Exit criteria

All automated checks green; no real runtime/process/input/network integration.
