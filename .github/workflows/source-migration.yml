name: Source migration

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  migrate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Explain canonical migration path
        run: echo 'Canonical source migration is performed by CI on push; this workflow is manual-only.'
